TASK: Select the appropriate workflow.

IF user says:

* "improve abstract" → run abstract pipeline
* "review paper" → run paper_wide_audit_agent
* "fix issues" → run fix pipeline

Always load:

* STYLE_LOCK.md
