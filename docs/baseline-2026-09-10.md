# Acceptance baseline — 2026-09-10

Captured for Phase 0 of [branch-integration-plan.md](branch-integration-plan.md). This is the
bar the merged tree must clear: **the result must be at least as good as the best column here,
example by example.**

Method: `tests/TestExamplesSmoke.m` copied into a detached worktree of each branch and run under
`matlab -batch` with figures suppressed. Read-only for the study branches — nothing was committed
to them (see the write policy in §3 of the plan).

## Examples

| Example | `develop` (repaired) | `Vibrations` | `CAS_Arm` |
|---|---|---|---|
| `elasticity/planeProblems/ConstStressTest.m` | **PASS** | FAIL | PASS |
| `elasticity/solidProblems/ConstStressSolidTest.m` | **PASS** | FAIL | PASS |
| `elasticity/planeProblems/CantileverTest.m` | **PASS** | FAIL | PASS |
| `elasticity/planeProblems/LameProblemTest.m` | **PASS** | FAIL | FAIL |
| `elasticity/solidProblems/CantileverSolidTest.m` | **PASS** | FAIL | PASS |
| `elasticity/planeProblems/InclinedSupportTest.m` | **PASS** | FAIL | PASS |
| | **6/6** | **0/6** | **5/6** |

> **Seventh example added 2026-09-11.** `elasticity/planeProblems/FrameOnElasticGround.m` joins
> the smoke set, and it is the one this baseline said was missing — see "Multi-group models"
> below. It mixes a `Frame2D` group (`ux,uy,fiz`) with a `PlaneStressElem` group (`ux,uy`) on one
> mesh, so it is the first entry that fails if per-node DOF unioning breaks. develop is now
> **7/7**. It runs in 0.4 s. It exists only on develop, and the harness skips absent examples, so
> the feature-branch columns are unaffected and this baseline still reads as captured.

Two results are worth stating plainly.

**`Vibrations` fails all six.** Not a regression in these examples — it is the consequence of
`LinearElasticityWeighted` being folded into `LinearElasticity` in `334fa7f` without migrating
callers. All six fixtures reference the removed class, as do 51 files in total. Vibrations'
*own* examples (`examples/Pillar/quickTest.m`) pass; these shared ones do not. Any Vibrations
baseline has to be taken on its native examples, which is why the merge acceptance test is the
**union** of what each branch passes, not the intersection.

**`develop` now passes more of these than either feature branch.** After the Phase 1 repair it
is the only branch that runs `LameProblemTest`. That inverts the situation the survey started
from and means develop, not CAS_Arm, is the better base for the merged tree.

## Unit tests on `develop`

| Suite | Result |
|---|---|
| `TestConstantStressPatch` | 6/6 pass |
| `TestExamplesSmoke` | 6/6 pass |
| `TestJ2PlaneStressReturnMapping` | 5/6 pass |
| `TestMultiGroupAssembly` *(added 2026-09-10, after this baseline)* | 6/6 pass |
| **Total** | **17 passed, 1 failed** *(23 passed, 1 failed with the added suite)* |

The single failure is `equivalentPlasticStrainIsScalarNotBroadcast` — the scalar accumulated
equivalent plastic strain being broadcast into the 3-component `pstrain` slot. That is a
state-variable design question rather than an error (Box 9.4 keeps the plastic strain tensor and
ε̄p as distinct variables), so it is recorded under Rule 2 rather than fixed during the merge.

## Not yet captured — numerical references

This baseline records **what runs**, not **what the numbers are**. A silently wrong stiffness
matrix still converges to something plausible and would pass every row above. The numerical
oracle described in Phase 0 — final compliance, volume fraction and iteration count for
`Cantilever.m`, `Beam99.m` and `Lshape.m` on `main` — is still outstanding, and gates Phase 5
rather than Phase 1.

Note that `main` needs its one-line `plotSupport` fix applied in the worktree before it will
run; that fix is already present on develop and must not be committed to `main`, which is
read-only.

## Still broken on `develop`

Five model fixtures remain un-migrated to the Mesh API, all genuinely multi-block — they merge
several mesh additions into one mesh, which is the multi-element case the DOF refactor exists to
support and needs judgement rather than a mechanical transform:

`ChocolateModel.m` · `CorbelModelLinear.m` · `Pylon2DModel.m` · `PylonModel.m` ·
`SpecimenModelLinear.m`

### Multi-group models — one found and fixed

The list above is about *mesh* blocks. A separate case is models with several **element groups**
on one mesh, which this baseline did not probe because every example in the smoke set has
exactly one group.

`CorbelModelMultiMat.m` builds `{fe1 fe2 fe3}` — three materials on one mesh — and called
`solveWeighted` against a `felems` cell array whose orientation `FEAnalysis` mis-indexed, so it
sized its density vector to the first group and assembled only that group. Fixed in `2969edc`
and covered by `TestMultiGroupAssembly`; see the error table in
[integration-rules.md](integration-rules.md).

This is the capability develop's DOF refactor exists to provide, so it is worth stating plainly
that **the baseline above did not test it** — the acceptance bar it sets is a single-element-group
bar.
